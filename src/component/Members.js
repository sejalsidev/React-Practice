import React from 'react'

const Members = (props) => {
  return (
    <div>
        <h1>GetData component</h1>
        <button onClick={props.data}>Call a function</button>
    </div>

  )
}

export default Members